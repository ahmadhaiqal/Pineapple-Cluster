apiVersion: apps/v1
kind: Deployment
metadata:
  name: cloudflared-sonarr
spec:
  selector:
    matchLabels:
      app: cloudflared-sonarr
  replicas: 2
  template:
    metadata:
      labels:
        app: cloudflared-sonarr
    spec:
      containers:
        - name: cloudflared
          image: cloudflare/cloudflared:2026.6.0
          args:
            - tunnel
            - --config
            - /etc/cloudflared/config/config.yaml
            - run
          livenessProbe:
            httpGet:
              path: /ready
              port: 2000
            failureThreshold: 1
            initialDelaySeconds: 10
            periodSeconds: 10
          volumeMounts:
            - name: config
              mountPath: /etc/cloudflared/config
              readOnly: true
            - name: creds
              mountPath: /etc/cloudflared/creds
              readOnly: true
      volumes:
        - name: creds
          secret:
            secretName: tunnel-credentials-sonarr
        - name: config
          configMap:
            name: cloudflared-sonarr
            items:
              - key: config.yaml
                path: config.yaml

---
apiVersion: v1
kind: ConfigMap
metadata:
  name: cloudflared-sonarr
data:
  config.yaml: |
    tunnel: sonarr

    credentials-file: /etc/cloudflared/creds/credentials.json

    metrics: 0.0.0.0:2000
    no-autoupdate: true

    ingress:
    - hostname: sonarr.amdhql.org
      service: http://sonarr:8989

    - service: http_status:404
