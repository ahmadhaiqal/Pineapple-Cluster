apiVersion: apps/v1
kind: Deployment
metadata:
  name: cloudflared-jellyfin
spec:
  selector:
    matchLabels:
      app: cloudflared-jellyfin
  replicas: 2
  template:
    metadata:
      labels:
        app: cloudflared-jellyfin
    spec:
      containers:
        - name: cloudflared
          image: cloudflare/cloudflared:2026.6.1
          args:
            - tunnel
            - --protocol
            - http2
            - --config
            - /etc/cloudflared/config/config.yaml
            - run
          livenessProbe:
            httpGet:
              path: /ready
              port: 2000
            failureThreshold: 1
            initialDelaySeconds: 10
            periodSeconds: 10
          volumeMounts:
            - name: config
              mountPath: /etc/cloudflared/config
              readOnly: true
            - name: creds
              mountPath: /etc/cloudflared/creds
              readOnly: true
      volumes:
        - name: creds
          secret:
            secretName: tunnel-credentials-jellyfin
        - name: config
          configMap:
            name: cloudflared-jellyfin
            items:
              - key: config.yaml
                path: config.yaml

---
apiVersion: v1
kind: ConfigMap
metadata:
  name: cloudflared-jellyfin
data:
  config.yaml: |
    tunnel: jellyfin

    credentials-file: /etc/cloudflared/creds/credentials.json

    metrics: 0.0.0.0:2000
    no-autoupdate: true

    ingress:
    - hostname: jellyfin.amdhql.org
      service: http://jellyfin:8096

    - service: http_status:404
