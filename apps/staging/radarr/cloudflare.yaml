apiVersion: apps/v1
kind: Deployment
metadata:
  name: cloudflared-radarr
spec:
  selector:
    matchLabels:
      app: cloudflared-radarr
  replicas: 2
  template:
    metadata:
      labels:
        app: cloudflared-radarr
    spec:
      containers:
        - name: cloudflared
          image: cloudflare/cloudflared:latest
          args:
            - tunnel
            - --config
            - /etc/cloudflared/config/config.yaml
            - run
          livenessProbe:
            httpGet:
              path: /ready
              port: 2000
            failureThreshold: 1
            initialDelaySeconds: 10
            periodSeconds: 10
          volumeMounts:
            - name: config
              mountPath: /etc/cloudflared/config
              readOnly: true
            - name: creds
              mountPath: /etc/cloudflared/creds
              readOnly: true
      volumes:
        - name: creds
          secret:
            secretName: tunnel-credentials-radarr
        - name: config
          configMap:
            name: cloudflared-radarr
            items:
              - key: config.yaml
                path: config.yaml

---
apiVersion: v1
kind: ConfigMap
metadata:
  name: cloudflared-radarr
data:
  config.yaml: |
    tunnel: radarr

    credentials-file: /etc/cloudflared/creds/credentials.json

    metrics: 0.0.0.0:2000
    no-autoupdate: true

    ingress:
    - hostname: radarr.amdhql.org
      service: http://radarr:7878

    - service: http_status:404
