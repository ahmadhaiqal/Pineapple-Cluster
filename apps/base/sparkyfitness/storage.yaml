apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: sparkyfitness-data-pvc
spec:
  storageClassName: "longhorn"
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
