apiVersion: apps/v1
kind: Deployment
metadata:
  name: radarr
spec:
  replicas: 1
  selector:
    matchLabels:
      app: radarr
  template:
    metadata:
      labels:
        app: radarr
    spec:
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
              - matchExpressions:
                  - key: kubernetes.io/hostname
                    operator: In
                    values:
                      - urial-lab

      securityContext:
        fsGroup: 1000

      initContainers:
        - name: init-dirs
          image: busybox:latest
          command: ["sh", "-c", "mkdir -p /data/media/movies /data/torrents/movies"]
          volumeMounts:
            - name: data
              mountPath: /data

      containers:
        - name: radarr
          image: lscr.io/linuxserver/radarr:latest
          ports:
            - containerPort: 7878
              protocol: TCP

          env:
            - name: PUID
              value: "1000"
            - name: PGID
              value: "1000"
            - name: TZ
              value: "Asia/Kuala_Lumpur"

          securityContext:
            allowPrivilegeEscalation: false

          lifecycle:
            postStart:
              exec:
                command:
                  - sh
                  - -c
                  - |
                    until [ -f /config/config.xml ] && \
                      KEY=$(sed -n 's|.*<ApiKey>\(.*\)</ApiKey>.*|\1|p' /config/config.xml) && \
                      curl -sf http://localhost:7878/api/v3/system/status \
                        -H "X-Api-Key: $KEY" > /dev/null 2>&1; do
                      sleep 5
                    done
                    curl -sf http://localhost:7878/api/v3/rootfolder \
                      -H "X-Api-Key: $KEY" | grep -q '/data/media/movies' || \
                    curl -sf -X POST http://localhost:7878/api/v3/rootfolder \
                      -H "X-Api-Key: $KEY" -H "Content-Type: application/json" \
                      -d '{"path":"/data/media/movies"}'

          volumeMounts:
            - name: radarr-config
              mountPath: /config
            - name: data
              mountPath: /data

      restartPolicy: Always

      volumes:
        - name: radarr-config
          persistentVolumeClaim:
            claimName: radarr-config-pvc
        - name: data
          persistentVolumeClaim:
            claimName: media-pvc
