---
extends: default

ignore: |
  cluster/staging/flux-system/gotk-components.yaml
  cluster/staging/flux-system/kustomization.yaml

rules:
  line-length:
    max: 120
    level: warning
  document-start:
    present: false
  comments:
    min-spaces-from-content: 1
