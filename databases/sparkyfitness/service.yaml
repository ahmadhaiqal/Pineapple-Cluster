apiVersion: v1
kind: Service
metadata:
  name: sparkyfitness-db-lb
  namespace: sparkyfitness
  annotations:
    external-dns.alpha.kubernetes.io/hostname: sparkyfitness-db.amdhql.org
  labels:
    cilium-lb-pool: pineapple-pool
spec:
  type: LoadBalancer
  ports:
    - port: 5432
      targetPort: 5432
  selector:
    cnpg.io/cluster: sparkyfitness-db-production-cnpg-v1
    role: primary
